#!/bin/zsh
if [[ -s "$HOME/.rvm/environments/ruby-1.9.3-p125@psychedelica" ]] ; then
  . "$HOME/.rvm/environments/ruby-1.9.3-p125@psychedelica"
else
  rvm --create use  "ruby-1.9.3-p125@psychedelica"
fi
